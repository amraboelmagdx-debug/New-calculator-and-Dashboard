# Local MongoDB for the Calculator app (product catalog cache + roles, etc.)
services:
  mongo:
    image: mongo:7
    container_name: calculator-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - calculator_mongo_data:/data/db

volumes:
  calculator_mongo_data:
